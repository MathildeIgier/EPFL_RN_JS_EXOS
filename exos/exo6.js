//Write 'all' function and check that all the number of an array are higher than 1 or lower than 1

const all = (arr, fn) => {
  //Write function here
}
console.log(all([4, 2, 3], x => x > 1)); 
console.log(all([4, 2, 3], x => x < 1));