//Solution
const giveHigherNumber = (num1, num2) => {
  return num1 >= num2 ? num1 : num2
};

const res = giveHigherNumber(16, 89);
console.log(res)

//Shorter solution
const giveHigherNumber = (num1, num2) => num1 >= num2 ? num1 : num2;

const res = giveHigherNumber(16, 89);
console.log(res)